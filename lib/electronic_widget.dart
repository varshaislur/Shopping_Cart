import 'package:flutter/material.dart';

class ElectronicWidget extends StatelessWidget {
  const ElectronicWidget({super.key, required this.electronicItem});
  final ElectronicWidget electronicItem;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
